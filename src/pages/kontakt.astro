---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Kontakt – TSG Messel Fußball">
	<div class="page contact-page">
		<header>
			<p class="eyebrow">Kontakt</p>
			<h1>Schreib uns eine Nachricht</h1>
			<p>
				Du hast Interesse am Fußball bei der TSG Messel oder möchtest ein Probetraining vereinbaren?
				Nutze das Formular und wir melden uns schnellstmöglich bei dir.
			</p>
		</header>

		<form class="contact-form" method="POST" action="https://formspree.io/f/xkogdjpo">
			<input type="hidden" name="_subject" value="Kontaktanfrage TSG Messel Fußball" />
			<div class="form-row">
				<label>
					Vor- und Nachname
					<input type="text" name="name" required />
				</label>
				<label>
					E-Mail-Adresse
					<input type="email" name="email" required />
				</label>
			</div>

			<label>
				Telefon (optional)
				<input type="tel" name="phone" />
			</label>

			<label>
				Interesse an
				<select name="bereich" required>
					<option value="">Bitte wählen</option>
					<option value="jugend">Jugend</option>
					<option value="herren">Herren</option>
					<option value="ah">Alte Herren</option>
					<option value="sonstiges">Sonstiges</option>
				</select>
			</label>

			<label>
				Nachricht
				<textarea name="nachricht" rows="5" required></textarea>
			</label>

			<button type="submit" class="btn">Nachricht senden</button>
		</form>
	</div>
</BaseLayout>

<style>
.page {
	max-width: 720px;
	margin: 0 auto;
	padding: 2.5rem 1.5rem 3rem;
	display: flex;
	flex-direction: column;
	gap: 1.5rem;
}

.eyebrow {
	text-transform: uppercase;
	letter-spacing: 0.18em;
	font-size: 0.8rem;
	color: rgba(15, 23, 42, 0.6);
	margin-bottom: 0.5rem;
}

.contact-form {
	display: flex;
	flex-direction: column;
	gap: 1rem;
	padding: 1.5rem;
	border-radius: 24px;
	background: #fff;
	box-shadow: 0 20px 40px rgba(15, 23, 42, 0.08);
}

.form-row {
	display: grid;
	grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
	gap: 1rem;
}

label {
	display: flex;
	flex-direction: column;
	gap: 0.4rem;
	font-weight: 600;
	font-size: 0.9rem;
}

input,
select,
textarea {
	border-radius: 12px;
	border: 1px solid rgba(15, 23, 42, 0.15);
	padding: 0.75rem 0.9rem;
	font-size: 1rem;
	font-family: inherit;
}

textarea {
	resize: vertical;
}

.btn {
	align-self: flex-start;
	display: inline-flex;
	align-items: center;
	justify-content: center;
	padding: 0.75rem 1.7rem;
	border-radius: 999px;
	border: none;
	background: #1d4ed8;
	color: #fff;
	font-weight: 600;
	cursor: pointer;
}
</style>
