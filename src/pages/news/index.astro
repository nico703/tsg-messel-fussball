---
import BaseLayout from "../../layouts/BaseLayout.astro";

const news = [
  {
    title: "1. Fußball-Camp 2026 bei der TSG Messel",
    date: "29. Dezember 2025",
    category: "Jugend",
    image: "/images/Grubi_fuba_logo.png",
    teaser:
      "In der ersten Sommerferienwoche 2026 bietet die TSG Messel erstmals ein professionelles Fußballcamp für Kinder und Jugendliche an. In Kooperation mit der Fußballfabrik erwartet die Teilnehmer eine unvergessliche Fußballwoche.",
  },
  {
    title: "Heimsieg der 1. Mannschaft",
    date: "02. Juni 2025",
    category: "Herren",
    image: "/images/news2.jpg",
    teaser:
      "Mit großem Einsatz und lautstarker Unterstützung von der Seitenlinie bleiben drei wichtige Punkte in Messel.",
  },
  {
    title: "Sommerfest der Fußballabteilung",
    date: "25. Mai 2025",
    category: "Verein",
    image: "/images/news3.jpg",
    teaser:
      "Grill, Musik, Spiele für Kinder und viele gute Gespräche – das Sommerfest zeigt, wie lebendig die TSG-Familie ist.",
  },
];
---

<BaseLayout
  title="News – TSG Messel Fußball"
  description="Aktuelle Meldungen, Spielberichte und Veranstaltungen der Fußballabteilung der TSG Messel."
>
  <section class="border-b border-[color:var(--tsg-border)] bg-[color:var(--tsg-surface)]">
    <div class="mx-auto max-w-6xl px-4 py-10 md:py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.25em] text-[color:var(--tsg-accent)]">
        Aktuelles
      </p>
      <h1 class="mt-3 text-3xl font-bold text-slate-900 md:text-4xl">News &amp; Berichte aus der Abteilung.</h1>
      <p class="mt-4 max-w-2xl text-sm text-[color:var(--tsg-accent)] md:text-base">
        Hier finden sich aktuelle Meldungen, Spielberichte, Turniere und besondere Ereignisse aus der Fußballabteilung
        der TSG Messel.
      </p>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-4 py-12 md:py-16">
    {news.length === 0 ? (
      <p class="text-sm text-slate-700">Derzeit liegen keine News-Einträge vor. Schau gerne später wieder vorbei.</p>
    ) : (
      <div class="grid gap-6 md:grid-cols-3">
        {news.map((item) => (
          <article class="flex flex-col overflow-hidden rounded-2xl border border-[color:var(--tsg-border)] bg-[color:var(--tsg-surface)] shadow-[0_16px_30px_rgba(30,64,175,0.08)]">
            <div class="h-40 w-full overflow-hidden">
              <img
                src={item.image}
                alt={item.title}
                class="h-full w-full object-cover"
                loading="lazy"
                decoding="async"
                width="600"
                height="300"
              />
            </div>
            <div class="flex flex-1 flex-col p-4 text-sm">
              <div class="flex items-center justify-between text-[0.7rem] text-slate-500">
                <span class="rounded-full bg-[color:var(--tsg-surface-muted)] px-2 py-0.5 font-semibold uppercase tracking-[0.16em] text-[color:var(--tsg-primary)]">
                  {item.category}
                </span>
                <span>{item.date}</span>
              </div>
              <h2 class="mt-3 text-sm font-semibold text-slate-900 md:text-base">{item.title}</h2>
              <p class="mt-2 flex-1 text-sm text-slate-700">{item.teaser}</p>
            </div>
          </article>
        ))}
      </div>
    )}
  </section>
</BaseLayout>
